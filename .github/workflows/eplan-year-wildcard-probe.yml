name: ePlan year wildcard probe

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/eplan-year-wildcard-probe.yml"
      - "scripts/probe-eplan-year-wildcards.mjs"
  workflow_dispatch:

permissions: { contents: read }

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - run: node scripts/probe-eplan-year-wildcards.mjs
